```{r}
library(tidyverse)
library(lubridate)
```

```{r}
prisons <- read.csv("prisons.csv")
```

```{r}
prisons <- prisons %>% mutate(date_format = parse_date_time(Date.Signed, orders="mdy"))
```

```{r}
prisons %>% ggplot(aes(x=date_format)) +
  geom_density()
```

